// require the array from seed.js

// mongoose insertMany
  // require index.js OR schema.js? (want access to models)
  // var data = [ { field: 'value', field: 'value'}];
  // Rating.inserMany(data, function(err, result) {
  //   if (err) {
  //     console.log(err);
  //   }

  //   console.log('number of documents inserted:', result.insertedCount);
  // });
    // insertMany returns an object with some properties
    // ex. result.insertedCount (line 11)

// time the seeding process for journal